function checkFisrtTime(){
    //check to see if they are here for the first time
    const hasVisited = localStorage.getItem('hasVisited');


    if (!hasVisited) {
        //onboard the user and ask goals and steps
    }
}